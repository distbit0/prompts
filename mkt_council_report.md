pls rank mkts/events by swap qty per day (use market launch date of each mkt to determine how long it has been running for), volume per day and tvl (one ranked list for metric, incl. the metric value for each mkt)

If `$ARGUMENTS` is non-empty, treat it as a filename, read the input text from that file, and write the output back to that file.

If `$ARGUMENTS` is empty, get the input text from my clipboard and copy the output to my clipboard once you have finished, using `xclip -selection clipboard`.